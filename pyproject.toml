[project]
name = "care-dem"
version = "0.1.0"
description = "CareDem: Compassionate AI for dementia care"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
